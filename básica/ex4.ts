/*2- Exibir os números pares de 0 a 60, fazer uso do laço
de repetição for*/

let par : number = 0;

for (let x = 0; x <= 30; x++){
   console.log(`Os números são: ${par}`);  
   par += 2 ;// par = par + 2
}